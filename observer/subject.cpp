
#include "subject.h"

std::string getValue() {
	// Récupérer la valeur du sujet
};
void setValue(std::string value) {
	// Définir la valeur du sujet
};

void registerObserver(Observer*o){
	// Enregistrer un observateur
};

bool isRegistered(Observer*o){
	// Vérifier si un observateur est enregistré
};

void unregisterObserver(Observer*o) {
	// Retirer un observateur qui est enregistré
};